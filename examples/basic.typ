#import "@preview/todone:0.1.0": *

#show: todone

= Quarterly report draft

The revenue figures in section 2 are taken from the preliminary close and
need to be reconciled against the audited statements before circulation.
#todo[Replace placeholder numbers in Table 2 once audit is final @alice]

Our regional breakdown still references the old three-region split. The
new structure has four regions, and the map on page 6 should be updated
to match. #fixme[Redraw regional map with the new four-region split @bob]

The executive summary mentions a launch date that has since slipped by
two weeks. #todo[Push launch date in summary to match the latest plan @alice @carol]

In the appendix, the methodology paragraph still refers to an internal
codename rather than the published product name.
#ask[Is "Atlas" still the working name, or did marketing settle on something @dave]

Finally, the citation style is inconsistent between sections 3 and 4.
#fixme[Unify citation style across all sections @carol]

= TODOs

#todo-list()
